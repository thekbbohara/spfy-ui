{
  "name": "spfyui",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && esbuild ./dist/index.js --bundle --format=esm --platform=node --minify --outfile=dist/index.mini.js",
    "lint": "tsc",
    "start": "bun run src/index.ts",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "react-ui",
    "react-icons",
    "nextjs-icons",
    "react-svg-element",
    "react-component",
    "svg-scrapper",
    "components-scrapper"
  ],
  "author": "thekbbohara",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.5.0",
    "esbuild": "^0.23.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "-": "^0.0.1",
    "fast-xml-parser": "^4.4.1",
    "puppeteer": "^23.1.1"
  }
}
